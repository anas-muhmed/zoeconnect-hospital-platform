services:
  # ══════════════════════════════════════════════════════════════════════
  # Nginx Self-Hosted Configuration Override
  # ══════════════════════════════════════════════════════════════════════
  hdsp-nginx:
    volumes:
      - ./docker/nginx/conf.d/self-hosted.conf:/etc/nginx/conf.d/default.conf:ro
