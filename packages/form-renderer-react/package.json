{
  "name": "@hdsp/form-renderer-react",
  "version": "0.1.0",
  "private": true,
  "description": "Runtime fill/submit React renderer (ADR-003's Renderer half of Builder/Renderer separation). Depends ONLY on @hdsp/form-schema — never on @hdsp/canvas-engine or canvas-engine-react, since Runtime has no scene graph, no undo/redo, no Designer chrome. Milestone 4 (Runtime, docs/architecture/MILESTONE_PLAN.md).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hdsp/form-schema": "file:../form-schema"
  },
  "peerDependencies": {
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  }
}
