{
  "$comment": "Phase 12 (Task 12.5) — machine-readable Backend<->Connector compatibility matrix. Source of truth for infrastructure/installer/check-compatibility.js and build-images.yml's version-manifest job. Human-readable narrative lives in VERSIONING.md — keep both in sync when adding a row.",
  "protocolVersion": "1",
  "minCompatibleConnectorVersion": "0.1.0",
  "matrix": [
    {
      "connectorVersionRange": "0.1.x",
      "protocolVersion": "1",
      "compatibleBackendVersionRange": "1.0.x",
      "notes": "Phase 6 standalone build, Phase 7 CloudOracleTransport relay (one conformance template: SELECT 1 FROM dual), Phase 9 REDIS_TLS support. Pairing-key credential (Phase 10, Task 10.4) is generated by the backend but NOT YET consumed at the protocol level by this Connector version — see TenantConnectorPairing entity's doc comment. Do not treat 'compatible' here as 'pairing-authenticated'; it means 'the Message Transport request/response shape matches', nothing more."
    }
  ]
}
